package com.yoo.mypm.ui.theme

import androidx.compose.ui.graphics.Color

// MYPM 빨강 계열 색상 시스템
// 계층별 색상 (진함 → 연함)
val RedLevel1 = Color(0xFFFF0000)  // 최종 목표 (100% 채도)
val RedLevel2 = Color(0xFFFF3333)  // 반기별 (80% 채도)
val RedLevel3 = Color(0xFFFF6666)  // 분기별 (60% 채도)
val RedLevel4 = Color(0xFFFF9999)  // 월별 (40% 채도)
val RedLevel5 = Color(0xFFFFCCCC)  // 주별 (20% 채도)
val RedLevel6 = Color(0xFFFFE6E6)  // 일별 (10% 채도)

// 메인 색상
val PrimaryRed = Color(0xFFFF0000)
val PrimaryRedLight = Color(0xFFFF6666)
val PrimaryRedDark = Color(0xFFCC0000)

// 배경 색상
val BackgroundLight = Color(0xFFFFFFFF)  // 순백
val BackgroundDark = Color(0xFF1C1B1F)

// 표면 색상
val SurfaceLight = Color(0xFFFFFBFE)
val SurfaceDark = Color(0xFF1C1B1F)

// 텍스트 색상
val OnBackground = Color(0xFF000000)  // 순검정
val OnBackgroundDark = Color(0xFFE6E1E5)

// 보조 색상
val Gray = Color(0xFF757575)
val LightGray = Color(0xFFF5F5F5)
val DividerGray = Color(0xFFE0E0E0)